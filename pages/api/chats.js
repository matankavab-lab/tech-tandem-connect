import path from 'path'; import {promises as fs} from 'fs'; export default async function handler(req,res){ const file=path.join(process.cwd(),'data','seed.json'); const content=await fs.readFile(file,'utf8'); const data=JSON.parse(content); res.status(200).json(data.chats); }